import axios, {AxiosInstance, AxiosRequestConfig} from "axios";

const defaultInstance = axios.create({});

const bareAxiosFetcher = async (url: string, options: AxiosRequestConfig = {}, instance: AxiosInstance = defaultInstance) => {
    return await instance({
        url,
        ...options,
    }).then(r => r.data);
};

const mutationAxiosFetcher = async (url: string, options: AxiosRequestConfig = {}, instance: AxiosInstance = defaultInstance) => {
    return await instance({
        url,
        ...options,
    }).then(r => r.data);
};



const axiosAdaptor = {bare: bareAxiosFetcher, mutation: mutationAxiosFetcher};
export default axiosAdaptor;
