const defaultOptions = {
    method: "POST"
};



const fetchAdaptor = async (url:string , date:any , options:RequestInit = {}) => {
    const response = await fetch(url, {body:date , ...defaultOptions, ...options , });
    return response.json();

};

export default fetchAdaptor
